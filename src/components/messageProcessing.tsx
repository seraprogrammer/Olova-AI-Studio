import { ThinkingBlock } from "./ThinkingBlock"; // Adjust the path if needed

export function processThinkingContent(content: string) {
  // Your existing implementation using ThinkingBlock
  return <ThinkingBlock content={content} />;
}
